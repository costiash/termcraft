---
name: harness-audit
---

`relay/tools/setup_wizard.py` is the installer our operators run today and people keep getting into bad states with it. Audit it as an operator harness: explore it for real, give me a gap table with evidence from actual runs, fix what you find (keeping it a single script operators can run the same way), and verify the fixed version. Pick any theme default yourself.
